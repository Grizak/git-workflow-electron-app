// src/renderer.js
// This file is only meant for webpack to have a renderer entry point,
// It is not doing ANYTHING at all, but it has to be here because webpack
// will complain otherwise
console.log("Renderer bundle initialized");
